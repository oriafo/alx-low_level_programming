#include "main.h"

/**
 * _abs - return absolute value
 * @num : number to check
 * Return: return the absolute value
*/

int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (num * -1);
}
